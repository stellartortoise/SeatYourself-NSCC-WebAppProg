@model IEnumerable<SeatYourself.Models.Ticket>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.vOccasion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.vSeat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @* @Html.DisplayFor(modelItem => item.vOccasion.OccasionId) *@
                @item.vOccasion?.OccasionId
            </td>
            <td>
                @* @Html.DisplayFor(modelItem => item.vSeat.SeatId) *@
                @item.vSeat?.SeatId
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TicketId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TicketId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TicketId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
